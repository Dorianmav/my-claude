export const MERMAID_CONFIG = {
  startOnLoad: true,
  theme: "default",
  securityLevel: "loose",
  fontFamily: "sans-serif",
} as const;

export const PREVIEW_CONTAINER_STYLE = {
  width: "100%",
  height: "400px",
} as const;

export const PREVIEW_INNER_STYLE = {
  minHeight: "300px",
} as const;
